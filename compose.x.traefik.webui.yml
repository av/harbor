# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  webui:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.webui.rule=Host(`webui.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.webui.loadbalancer.server.port=${HARBOR_WEBUI_HOST_PORT}"
      - "traefik.http.routers.webui.entrypoints=web"
      - "traefik.http.routers.webui.service=webui"

    networks:
      - traefik-public

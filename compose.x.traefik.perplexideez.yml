# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  perplexideez:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.perplexideez.rule=Host(`perplexideez.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.perplexideez.loadbalancer.server.port=${HARBOR_PERPLEXIDEEZ_HOST_PORT}"
      - "traefik.http.routers.perplexideez.entrypoints=web"
      - "traefik.http.routers.perplexideez.service=perplexideez"

    networks:
      - traefik-public

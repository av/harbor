# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  mikupad:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.mikupad.rule=Host(`mikupad.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.mikupad.loadbalancer.server.port=${HARBOR_MIKUPAD_HOST_PORT}"
      - "traefik.http.routers.mikupad.entrypoints=web"
      - "traefik.http.routers.mikupad.service=mikupad"

    networks:
      - traefik-public

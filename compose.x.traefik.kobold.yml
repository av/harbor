# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  kobold:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.kobold.rule=Host(`kobold.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.kobold.loadbalancer.server.port=5001"
      - "traefik.http.routers.kobold.entrypoints=websecure"
      - "traefik.http.routers.kobold.tls=true"
      - "traefik.http.routers.kobold.service=kobold"

    networks:
      - traefik-public

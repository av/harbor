# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  tgi:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.tgi.rule=Host(`tgi.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.tgi.loadbalancer.server.port=${HARBOR_TGI_HOST_PORT}"
      - "traefik.http.routers.tgi.entrypoints=web"
      - "traefik.http.routers.tgi.service=tgi"

    networks:
      - traefik-public

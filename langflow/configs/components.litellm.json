{
  "llms": [
    {
      "provider": "openai",
      "models": [
        {
          "name": "gpt-3.5-turbo",
          "api_base": "${LANGFLOW_OPENAI_API_BASE}",
          "api_key": "${LANGFLOW_OPENAI_API_KEY}"
        },
        {
          "name": "gpt-4",
          "api_base": "${LANGFLOW_OPENAI_API_BASE}",
          "api_key": "${LANGFLOW_OPENAI_API_KEY}"
        }
      ]
    }
  ]
}

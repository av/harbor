# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  vllm:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.vllm.rule=Host(`vllm.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.vllm.loadbalancer.server.port=${HARBOR_VLLM_HOST_PORT}"
      - "traefik.http.routers.vllm.entrypoints=web"
      - "traefik.http.routers.vllm.service=vllm"

    networks:
      - traefik-public

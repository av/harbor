# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  sglang:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.sglang.rule=Host(`sglang.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.sglang.loadbalancer.server.port=30000"
      - "traefik.http.routers.sglang.entrypoints=websecure"
      - "traefik.http.routers.sglang.tls=true"
      - "traefik.http.routers.sglang.service=sglang"

    networks:
      - traefik-public

# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  autogpt:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.autogpt.rule=Host(`autogpt.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.autogpt.loadbalancer.server.port=${HARBOR_AUTOGPT_HOST_PORT}"
      - "traefik.http.routers.autogpt.entrypoints=web"
      - "traefik.http.routers.autogpt.service=autogpt"

    networks:
      - traefik-public

# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  langflow:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.langflow.rule=Host(`langflow.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.langflow.loadbalancer.server.port=${HARBOR_LANGFLOW_HOST_PORT}"
      - "traefik.http.routers.langflow.entrypoints=web"
      - "traefik.http.routers.langflow.service=langflow"

    networks:
      - traefik-public

# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  agent:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.agent.rule=Host(`agent.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.agent.loadbalancer.server.port=${HARBOR_AGENT_HOST_PORT}"
      - "traefik.http.routers.agent.entrypoints=web"
      - "traefik.http.routers.agent.service=agent"

    networks:
      - traefik-public

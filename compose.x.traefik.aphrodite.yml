# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  aphrodite:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.aphrodite.rule=Host(`aphrodite.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.aphrodite.loadbalancer.server.port=${HARBOR_APHRODITE_HOST_PORT}"
      - "traefik.http.routers.aphrodite.entrypoints=web"
      - "traefik.http.routers.aphrodite.service=aphrodite"

    networks:
      - traefik-public

# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  moltbot:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.moltbot.rule=Host(`moltbot.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.moltbot.loadbalancer.server.port=${HARBOR_MOLTBOT_HOST_PORT}"
      - "traefik.http.routers.moltbot.entrypoints=websecure"
      - "traefik.http.routers.moltbot.tls=true"
      - "traefik.http.routers.moltbot.service=moltbot"

    networks:
      - traefik-public

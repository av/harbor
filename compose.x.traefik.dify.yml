# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  dify:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.dify.rule=Host(`dify.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.dify.loadbalancer.server.port=${HARBOR_DIFY_HOST_PORT}"
      - "traefik.http.routers.dify.entrypoints=web"
      - "traefik.http.routers.dify.service=dify"

    networks:
      - traefik-public

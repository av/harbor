# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  optillm:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.optillm.rule=Host(`optillm.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.optillm.loadbalancer.server.port=${HARBOR_OPTILLM_HOST_PORT}"
      - "traefik.http.routers.optillm.entrypoints=web"
      - "traefik.http.routers.optillm.service=optillm"

    networks:
      - traefik-public

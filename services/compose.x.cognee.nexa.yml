services:
  cognee:
    depends_on:
      - nexa
    environment:
      - LLM_PROVIDER=openai
      - LLM_API_KEY=sk-nexa
      - LLM_MODEL=${HARBOR_NEXA_MODEL}
      - LLM_ENDPOINT=http://nexa:8000/v1

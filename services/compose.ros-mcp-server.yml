services:
  ros-mcp-server:
    container_name: ${HARBOR_CONTAINER_PREFIX}.ros-mcp-server
    build:
      context: ./services/ros-mcp-server
      args:
        GIT_REF: ${HARBOR_ROS_MCP_SERVER_GIT_REF}
    env_file:
      - ./.env
      - ./services/ros-mcp-server/override.env
    environment:
      - ROSBRIDGE_HOST=${HARBOR_ROS_MCP_SERVER_ROSBRIDGE_HOST:-host.docker.internal}
      - ROSBRIDGE_PORT=${HARBOR_ROS_MCP_SERVER_ROSBRIDGE_PORT:-9090}
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - harbor-network

# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  voicebox:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.voicebox.rule=Host(`voicebox.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.voicebox.loadbalancer.server.port=17493"
      - "traefik.http.routers.voicebox.entrypoints=websecure"
      - "traefik.http.routers.voicebox.tls=true"
      - "traefik.http.routers.voicebox.service=voicebox"

    networks:
      - traefik-public

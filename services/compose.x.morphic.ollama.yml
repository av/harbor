services:
  morphic:
    environment:
      - OLLAMA_BASE_URL=${HARBOR_OLLAMA_INTERNAL_URL}
      - NEXT_PUBLIC_OLLAMA_MODEL=${HARBOR_MORPHIC_MODEL}
      - NEXT_PUBLIC_OLLAMA_TOOL_CALL_MODEL=${HARBOR_MORPHIC_TOOL_MODEL}

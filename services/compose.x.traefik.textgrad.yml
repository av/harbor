# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  textgrad:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.textgrad.rule=Host(`textgrad.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.textgrad.loadbalancer.server.port=8888"
      - "traefik.http.routers.textgrad.entrypoints=websecure"
      - "traefik.http.routers.textgrad.tls=true"
      - "traefik.http.routers.textgrad.service=textgrad"

    networks:
      - traefik-public

# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  latitude:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.latitude.rule=Host(`latitude.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.latitude.loadbalancer.server.port=${HARBOR_LATITUDE_HOST_PORT}"
      - "traefik.http.routers.latitude.entrypoints=websecure"
      - "traefik.http.routers.latitude.tls=true"
      - "traefik.http.routers.latitude.service=latitude"

    networks:
      - traefik-public

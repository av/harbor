# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  npcsh:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.npcsh.rule=Host(`npcsh.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.npcsh.loadbalancer.server.port=5337"
      - "traefik.http.routers.npcsh.entrypoints=websecure"
      - "traefik.http.routers.npcsh.tls=true"
      - "traefik.http.routers.npcsh.service=npcsh"

    networks:
      - traefik-public

# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  needle:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.needle.rule=Host(`needle.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.needle.loadbalancer.server.port=7860"
      - "traefik.http.routers.needle.entrypoints=websecure"
      - "traefik.http.routers.needle.tls=true"
      - "traefik.http.routers.needle.service=needle"

    networks:
      - traefik-public

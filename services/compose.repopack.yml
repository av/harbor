services:
  repopack:
    build:
      context: ./services/repopack
      dockerfile: Dockerfile
    container_name: ${HARBOR_CONTAINER_PREFIX}.repopack
    env_file:
      - ./.env
      - ./services/repopack/override.env
    networks:
      - harbor-network
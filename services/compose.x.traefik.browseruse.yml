# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  browseruse:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.browseruse.rule=Host(`browseruse.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.browseruse.loadbalancer.server.port=7788"
      - "traefik.http.routers.browseruse.entrypoints=websecure"
      - "traefik.http.routers.browseruse.tls=true"
      - "traefik.http.routers.browseruse.service=browseruse"

    networks:
      - traefik-public

# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  stt:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.stt.rule=Host(`stt.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.stt.loadbalancer.server.port=8000"
      - "traefik.http.routers.stt.entrypoints=websecure"
      - "traefik.http.routers.stt.tls=true"
      - "traefik.http.routers.stt.service=stt"

    networks:
      - traefik-public

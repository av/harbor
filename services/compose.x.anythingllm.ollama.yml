services:
  anythingllm:
    environment:
      - LLM_PROVIDER=ollama
      - OLLAMA_BASE_PATH=${HARBOR_OLLAMA_INTERNAL_URL}
      - EMBEDDING_ENGINE=ollama
      - EMBEDDING_BASE_PATH=${HARBOR_OLLAMA_INTERNAL_URL}
  anythingllm-init:
    environment:
      - HARBOR_ANYTHINGLLM_DEFAULT_EMBEDDING_MODEL=nomic-embed-text:latest

# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  caddy:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.caddy.rule=Host(`caddy.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.caddy.loadbalancer.server.port=${HARBOR_CADDY_HTTP_HOST_PORT}"
      - "traefik.http.routers.caddy.entrypoints=websecure"
      - "traefik.http.routers.caddy.tls=true"
      - "traefik.http.routers.caddy.service=caddy"

    networks:
      - traefik-public

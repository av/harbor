services:
  llamacpp:
    image: ${HARBOR_LLAMACPP_IMAGE_NVIDIA}
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
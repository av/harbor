# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  promptfoo:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.promptfoo.rule=Host(`promptfoo.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.promptfoo.loadbalancer.server.port=${HARBOR_PROMPTFOO_HOST_PORT}"
      - "traefik.http.routers.promptfoo.entrypoints=web"
      - "traefik.http.routers.promptfoo.service=promptfoo"

    networks:
      - traefik-public

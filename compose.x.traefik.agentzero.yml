# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  agentzero:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.agentzero.rule=Host(`agentzero.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.agentzero.loadbalancer.server.port=${HARBOR_AGENTZERO_HOST_PORT}"
      - "traefik.http.routers.agentzero.entrypoints=web"
      - "traefik.http.routers.agentzero.service=agentzero"

    networks:
      - traefik-public

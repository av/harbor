# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  omniparser:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.omniparser.rule=Host(`omniparser.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.omniparser.loadbalancer.server.port=${HARBOR_OMNIPARSER_HOST_PORT}"
      - "traefik.http.routers.omniparser.entrypoints=web"
      - "traefik.http.routers.omniparser.service=omniparser"

    networks:
      - traefik-public

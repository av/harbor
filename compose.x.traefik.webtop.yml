# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  webtop:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.webtop.rule=Host(`webtop.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.webtop.loadbalancer.server.port=${HARBOR_WEBTOP_HOST_PORT}"
      - "traefik.http.routers.webtop.entrypoints=web"
      - "traefik.http.routers.webtop.service=webtop"

    networks:
      - traefik-public

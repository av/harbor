# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  boost:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.boost.rule=Host(`boost.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.boost.loadbalancer.server.port=8000"
      - "traefik.http.routers.boost.entrypoints=websecure"
      - "traefik.http.routers.boost.tls=true"
      - "traefik.http.routers.boost.service=boost"

    networks:
      - traefik-public

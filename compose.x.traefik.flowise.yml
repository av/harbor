# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  flowise:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.flowise.rule=Host(`flowise.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.flowise.loadbalancer.server.port=${HARBOR_FLOWISE_HOST_PORT}"
      - "traefik.http.routers.flowise.entrypoints=web"
      - "traefik.http.routers.flowise.service=flowise"

    networks:
      - traefik-public

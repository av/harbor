# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  hollama:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.hollama.rule=Host(`hollama.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.hollama.loadbalancer.server.port=${HARBOR_HOLLAMA_HOST_PORT}"
      - "traefik.http.routers.hollama.entrypoints=web"
      - "traefik.http.routers.hollama.service=hollama"

    networks:
      - traefik-public

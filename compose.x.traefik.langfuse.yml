# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  langfuse:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.langfuse.rule=Host(`langfuse.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.langfuse.loadbalancer.server.port=${HARBOR_LANGFUSE_HOST_PORT}"
      - "traefik.http.routers.langfuse.entrypoints=web"
      - "traefik.http.routers.langfuse.service=langfuse"

    networks:
      - traefik-public

# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  omnichain:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.omnichain.rule=Host(`omnichain.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.omnichain.loadbalancer.server.port=${HARBOR_OMNICHAIN_HOST_PORT}"
      - "traefik.http.routers.omnichain.entrypoints=web"
      - "traefik.http.routers.omnichain.service=omnichain"

    networks:
      - traefik-public

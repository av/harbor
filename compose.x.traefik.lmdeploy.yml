# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  lmdeploy:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.lmdeploy.rule=Host(`lmdeploy.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.lmdeploy.loadbalancer.server.port=${HARBOR_LMDEPLOY_HOST_PORT}"
      - "traefik.http.routers.lmdeploy.entrypoints=web"
      - "traefik.http.routers.lmdeploy.service=lmdeploy"

    networks:
      - traefik-public

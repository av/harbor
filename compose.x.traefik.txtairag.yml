# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  txtairag:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.txtairag.rule=Host(`txtairag.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.txtairag.loadbalancer.server.port=${HARBOR_TXTAI_RAG_HOST_PORT}"
      - "traefik.http.routers.txtairag.entrypoints=web"
      - "traefik.http.routers.txtairag.service=txtairag"

    networks:
      - traefik-public

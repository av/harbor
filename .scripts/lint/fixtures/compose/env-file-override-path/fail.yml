# expect-rule: env-file-override-path
# expect-hits: 2
services:
  alpha:
    image: alpine:latest
    container_name: ${HARBOR_CONTAINER_PREFIX}.alpha
    env_file:
      - ./.env
      - ./alpha.env
      - ./services/alpha/extra.env

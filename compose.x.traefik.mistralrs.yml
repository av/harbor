# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  mistralrs:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.mistralrs.rule=Host(`mistralrs.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.mistralrs.loadbalancer.server.port=${HARBOR_MISTRALRS_HOST_PORT}"
      - "traefik.http.routers.mistralrs.entrypoints=web"
      - "traefik.http.routers.mistralrs.service=mistralrs"

    networks:
      - traefik-public

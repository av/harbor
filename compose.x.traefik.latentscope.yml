# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  latentscope:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.latentscope.rule=Host(`latentscope.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.latentscope.loadbalancer.server.port=${HARBOR_LATENTSCOPE_HOST_PORT}"
      - "traefik.http.routers.latentscope.entrypoints=web"
      - "traefik.http.routers.latentscope.service=latentscope"

    networks:
      - traefik-public

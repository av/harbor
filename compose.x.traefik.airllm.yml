# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  airllm:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.airllm.rule=Host(`airllm.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.airllm.loadbalancer.server.port=5000"
      - "traefik.http.routers.airllm.entrypoints=websecure"
      - "traefik.http.routers.airllm.tls=true"
      - "traefik.http.routers.airllm.service=airllm"

    networks:
      - traefik-public

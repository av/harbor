# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  ol1:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.ol1.rule=Host(`ol1.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.ol1.loadbalancer.server.port=${HARBOR_OL1_HOST_PORT}"
      - "traefik.http.routers.ol1.entrypoints=web"
      - "traefik.http.routers.ol1.service=ol1"

    networks:
      - traefik-public

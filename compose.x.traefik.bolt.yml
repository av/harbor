# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  bolt:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.bolt.rule=Host(`bolt.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.bolt.loadbalancer.server.port=${HARBOR_BOLT_HOST_PORT}"
      - "traefik.http.routers.bolt.entrypoints=web"
      - "traefik.http.routers.bolt.service=bolt"

    networks:
      - traefik-public

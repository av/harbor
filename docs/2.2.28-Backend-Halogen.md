### [Halogen Flash Server](https://github.com/peonist-ai/halogen-flash-server)

> Handle: `halogen`<br/>
> API: [http://localhost:34950/v1](http://localhost:34950/v1)

Halogen serves Qwen3.8-Flash-Next through an OpenAI-compatible API on AMD Strix Halo (gfx1151). It is an API backend without a browser UI. It supports chat completions, streaming, tool calls and the Responses API.

#### Starting

Requires Linux, an AMD Strix Halo GPU with `/dev/kfd` and `/dev/dri`, and a 128 GB unified-memory machine. There is no CPU, NVIDIA or other AMD GPU fallback. Reserve at least 120 GiB of disk space for the model bundle, plus space for the container image. The model keeps a 47.7 GiB lookup table on disk and reads it through the host file cache. Harbor's defaults size the KV pool and prefill arena so that cache still has room when Open WebUI is also running. Other large loaded models on the same GPU can still starve that cache and stall the engine.

```bash
harbor pull --no-defaults halogen
harbor up --no-defaults halogen
```

First start downloads approximately 118 GiB into `services/halogen/models`. Downloads resume after interruption. Subsequent starts reuse the checkpoint, but a newer image may refresh its quality sidecar. Model loading takes several minutes after download. The healthcheck allows an hour for initial startup and probes the engine through the API.

Once healthy, list models and send a real request:

```bash
curl -fsS http://localhost:34950/v1/models
model=$(curl -fsS http://localhost:34950/v1/models | jq -r '.data[0].id')
jq -n --arg model "$model" \
  '{model: $model, messages: [{role: "user", content: "What is 2 + 2? Answer briefly."}], max_tokens: 256, temperature: 0}' \
  | curl -fsS http://localhost:34950/v1/chat/completions \
      -H 'Content-Type: application/json' --data-binary @-
```

#### Configuration

##### Environment Variables

Set these through [`harbor config`](./3.-Harbor-CLI-Reference.md#harbor-config), for example `harbor config set halogen.ctx 32768`:

| Variable | Default | Purpose |
| --- | --- | --- |
| `HARBOR_HALOGEN_HOST_PORT` | `34950` | Host API port |
| `HARBOR_HALOGEN_IMAGE` | `ghcr.io/peonist-ai/halogen-flash-server` | Upstream image repository |
| `HARBOR_HALOGEN_VERSION` | `0.6.3` | Image tag. 0.6.3 reads lookup-table rows in batches of 64 |
| `HARBOR_HALOGEN_WORKSPACE` | `./services/halogen` | Workspace containing the `models` directory |
| `HARBOR_HALOGEN_DOWNLOAD` | `peonist-ai/halogen-qwen3.8-flash-next` | Hugging Face model bundle to download when absent |
| `HARBOR_HALOGEN_CTX` | `262144` | Maximum context per request |
| `HARBOR_HALOGEN_KV_POOL_POSITIONS` | `262144` | Shared KV pool capacity across requests. This is one full native context. A larger pool steals file cache from the lookup table |
| `HARBOR_HALOGEN_KV_SLOTS` | `4` | Concurrent request slots. 4 is the measured acceptable engine batch on a 128 GiB Strix Halo, about 18 to 21 tok/s per stream. 8 raises total throughput a little and halves per-stream speed |
| `HARBOR_HALOGEN_MAX_TOK` | `16384` | Prefill chunk size, not the response token limit. A larger arena also steals file cache |
| `HARBOR_HALOGEN_HOST_RESERVE_GIB` | `20` | Host RAM the engine leaves for the OS and lookup-table file cache when it sizes the pool |
| `HARBOR_HALOGEN_MAX_TOKENS_DEFAULT` | `65536` | Chat completion budget when the client omits `max_tokens`. This covers thinking and visible tokens. The engine cap is also 65536 |
| `HARBOR_HALOGEN_ENGINE_WATCHDOG_S` | `900` | Seconds the engine may stay silent before the watchdog stops it. Keep this above 0 |

After preparing a complete model bundle, disable outbound downloads with `harbor config set halogen.download ''`. Point the workspace at the parent of an existing flat `models` directory to reuse it. A Hugging Face cache snapshot alone may contain symlinks whose targets are outside the mount.

Use `harbor env halogen` for additional upstream variables. For example, `harbor env halogen HALOGEN_VISION_TOWER 1` enables the vision sidecar included in the bundle. Contexts above 262144 require an explicit `HALOGEN_ROPE_YARN` factor and careful memory sizing. See upstream configuration before changing these settings.

##### Volumes

`${HARBOR_HALOGEN_WORKSPACE}/models` is mounted read-write at `/models`. It persists the checkpoint, tokenizer and sidecars across container removal. The upstream image controls ownership of downloaded files. Harbor does not remove them when stopping the service.

The container receives GPU devices, host IPC and unlimited locked memory as required by upstream. Only API port 8731 is published. The internal engine remains inside the container. The API has no authentication by default, so expose it only to trusted clients.

#### Integration with Harbor

```bash
harbor up --no-defaults webui halogen
```

Open WebUI waits for Halogen's healthcheck and receives `http://halogen:8731/v1` in its OpenAI configuration. Select the model exposed by Halogen. Other clients on the Harbor network can use the same URL with a placeholder key such as `sk-halogen`. Host clients use `http://localhost:34950/v1`.

#### Troubleshooting

```bash
harbor logs halogen
```

Logs follow by default. For a finite diagnostic snapshot, use `docker logs --tail 100 harbor.halogen` (adjust the container prefix if configured).

- Missing GPU devices: verify this is a supported Linux Strix Halo host with its AMD driver loaded.
- Out of memory or very slow prompts: free other model workloads and reduce the KV pool or prefill size. Lowering context alone does not lower an explicitly configured pool.
- Startup stuck on reserving the KV pool or extra serving slots, with compaction stalls and few contiguous 2 MiB host blocks: lower `halogen.kv_pool_positions` and `halogen.max_tok`.
- Chat stream starts then hangs, `/health` returns 503, and the engine watchdog stops a silent engine after unanswered PINGs. Image 0.6.3 plus the default 262144 pool and 16384 prefill arena exist so the lookup-table file cache is not starved. The default watchdog window is 900 seconds so a long decode does not look dead. Do not set `halogen.engine.watchdog.s` to 0. Do not treat a compose restart policy as the fix. Stop other GPU clients on `/dev/kfd` if the stall repeats.
- A reply stops in the thinking block with `finish_reason: length` and empty visible content: the client omitted `max_tokens` and an older default of 8192 ended the turn. Harbor now defaults that budget to 65536. Raise `halogen.max.tokens.default` only if you lowered it.
- WebUI looks stuck, tok/s drops, and logs say `beside other streams`: more than 4 slots share decode harder. The default is 4. Do not continue a long tool-search thread. Start a new chat.
- Download failure: check available disk space and model-directory permissions, then restart to resume.
- Unhealthy during a long first download: inspect progress in logs. Health becomes successful once download, model loading and the engine probe complete.

#### Links

- [GitHub repository and quickstart](https://github.com/peonist-ai/halogen-flash-server)
- [Upstream configuration](https://github.com/peonist-ai/halogen-flash-server/blob/main/docs/FLAGS.md)
- [Model bundle](https://huggingface.co/peonist-ai/halogen-qwen3.8-flash-next)
- [Upstream license](https://github.com/peonist-ai/halogen-flash-server/blob/main/LICENSE.md)

# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  sqlchat:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.sqlchat.rule=Host(`sqlchat.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.sqlchat.loadbalancer.server.port=${HARBOR_SQLCHAT_HOST_PORT}"
      - "traefik.http.routers.sqlchat.entrypoints=web"
      - "traefik.http.routers.sqlchat.service=sqlchat"

    networks:
      - traefik-public

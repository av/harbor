# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  parler:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.parler.rule=Host(`parler.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.parler.loadbalancer.server.port=${HARBOR_PARLER_HOST_PORT}"
      - "traefik.http.routers.parler.entrypoints=web"
      - "traefik.http.routers.parler.service=parler"

    networks:
      - traefik-public

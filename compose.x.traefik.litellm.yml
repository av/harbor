# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  litellm:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.litellm.rule=Host(`litellm.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.litellm.loadbalancer.server.port=${HARBOR_LITELLM_HOST_PORT}"
      - "traefik.http.routers.litellm.entrypoints=web"
      - "traefik.http.routers.litellm.service=litellm"

    networks:
      - traefik-public

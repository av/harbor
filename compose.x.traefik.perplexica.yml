# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  perplexica:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.perplexica.rule=Host(`perplexica.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.perplexica.loadbalancer.server.port=${HARBOR_PERPLEXICA_HOST_PORT}"
      - "traefik.http.routers.perplexica.entrypoints=web"
      - "traefik.http.routers.perplexica.service=perplexica"

    networks:
      - traefik-public

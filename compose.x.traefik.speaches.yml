# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  speaches:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.speaches.rule=Host(`speaches.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.speaches.loadbalancer.server.port=${HARBOR_SPEACHES_HOST_PORT}"
      - "traefik.http.routers.speaches.entrypoints=web"
      - "traefik.http.routers.speaches.service=speaches"

    networks:
      - traefik-public

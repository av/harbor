# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  morphic:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.morphic.rule=Host(`morphic.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.morphic.loadbalancer.server.port=${HARBOR_MORPHIC_HOST_PORT}"
      - "traefik.http.routers.morphic.entrypoints=web"
      - "traefik.http.routers.morphic.service=morphic"

    networks:
      - traefik-public

# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  raglite:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.raglite.rule=Host(`raglite.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.raglite.loadbalancer.server.port=${HARBOR_RAGLIT_HOST_PORT}"
      - "traefik.http.routers.raglite.entrypoints=web"
      - "traefik.http.routers.raglite.service=raglite"

    networks:
      - traefik-public

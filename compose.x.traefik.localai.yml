# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  localai:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.localai.rule=Host(`localai.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.localai.loadbalancer.server.port=${HARBOR_LOCALAI_HOST_PORT}"
      - "traefik.http.routers.localai.entrypoints=web"
      - "traefik.http.routers.localai.service=localai"

    networks:
      - traefik-public

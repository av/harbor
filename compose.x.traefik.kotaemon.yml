# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  kotaemon:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.kotaemon.rule=Host(`kotaemon.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.kotaemon.loadbalancer.server.port=7860"
      - "traefik.http.routers.kotaemon.entrypoints=websecure"
      - "traefik.http.routers.kotaemon.tls=true"
      - "traefik.http.routers.kotaemon.service=kotaemon"

    networks:
      - traefik-public

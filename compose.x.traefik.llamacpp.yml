# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  llamacpp:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.llamacpp.rule=Host(`llamacpp.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.llamacpp.loadbalancer.server.port=${HARBOR_LLAMACPP_HOST_PORT}"
      - "traefik.http.routers.llamacpp.entrypoints=web"
      - "traefik.http.routers.llamacpp.service=llamacpp"

    networks:
      - traefik-public

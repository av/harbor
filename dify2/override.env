# Harbor Dify2 Override Environment Variables
# These override the stock Dify .env.example with Harbor-specific values
# Order: .env (Harbor global) -> upstream/.env.example (Dify defaults) -> this file (overrides)

# Harbor-specific settings
HARBOR_DIFY2_HOST_PORT=3001

# Service hostnames - prefixed for Harbor's namespace isolation
DB_HOST=dify2-db_postgres
DB_PLUGIN_HOST=dify2-db_postgres
REDIS_HOST=dify2-redis
SSRF_PROXY_HTTP_URL=http://dify2-ssrf_proxy:3128
SSRF_PROXY_HTTPS_URL=http://dify2-ssrf_proxy:3128
SANDBOX_HOST=dify2-sandbox
PLUGIN_DAEMON_HOST=dify2-plugin_daemon
WEAVIATE_ENDPOINT=http://dify2-weaviate:8080

# Storage - use local filesystem (simplest for self-hosted)
STORAGE_TYPE=opendal
OPENDAL_SCHEME=fs
OPENDAL_FS_ROOT=/app/api/storage

# Vector store
VECTOR_STORE=weaviate

# Celery broker - prefixed redis
CELERY_BROKER_URL=redis://:difyai123456@dify2-redis:6379/1

# Plugin daemon
PLUGIN_DAEMON_URL=http://dify2-plugin_daemon:5003

# Sandbox
CODE_EXECUTION_ENDPOINT=http://dify2-sandbox:8194
SANDBOX_HTTP_PROXY=http://dify2-ssrf_proxy:3128
SANDBOX_HTTPS_PROXY=http://dify2-ssrf_proxy:3128

# Plugin daemon - use different port to avoid conflict with OrbStack
PLUGIN_DEBUGGING_PORT=5013
EXPOSE_PLUGIN_DEBUGGING_PORT=5013

# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  ldr:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.ldr.rule=Host(`ldr.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.ldr.loadbalancer.server.port=${HARBOR_LDR_HOST_PORT}"
      - "traefik.http.routers.ldr.entrypoints=web"
      - "traefik.http.routers.ldr.service=ldr"

    networks:
      - traefik-public

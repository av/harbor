# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  anythingllm:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.anythingllm.rule=Host(`anythingllm.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.anythingllm.loadbalancer.server.port=${HARBOR_ANYTHINGLLM_HOST_PORT}"
      - "traefik.http.routers.anythingllm.entrypoints=web"
      - "traefik.http.routers.anythingllm.service=anythingllm"

    networks:
      - traefik-public

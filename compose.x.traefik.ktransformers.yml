# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  ktransformers:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.ktransformers.rule=Host(`ktransformers.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.ktransformers.loadbalancer.server.port=${HARBOR_KTRANSFORMERS_HOST_PORT}"
      - "traefik.http.routers.ktransformers.entrypoints=web"
      - "traefik.http.routers.ktransformers.service=ktransformers"

    networks:
      - traefik-public

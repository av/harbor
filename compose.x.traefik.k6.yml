# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  k6:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.k6.rule=Host(`k6.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.k6.loadbalancer.server.port=${HARBOR_K6_HOST_PORT}"
      - "traefik.http.routers.k6.entrypoints=web"
      - "traefik.http.routers.k6.service=k6"

    networks:
      - traefik-public
